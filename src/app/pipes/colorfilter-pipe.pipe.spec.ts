import { ColorfilterPipePipe } from './colorfilter-pipe.pipe';

describe('ColorfilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorfilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
