import { BrandfilterPipePipe } from './brandfilter-pipe.pipe';

describe('BrandfilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BrandfilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
